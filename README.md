# Найди инвестора

[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python 3.12.7](https://img.shields.io/badge/Python-3.12.7-blue)](https://www.python.org/)

![Найди инвестора. Банк запустил линейку инновационных вложений — от мета вселенной до NFT-котов.  Ваша задача предсказать готовность клиентов банка инвестировать в новые инновационные проекты, используя данные предыдущих предложений
](img/rukod.png)

## Описание
Учебный проект на основе задачи "Найди инвестора" из соревнования по ML 2025 платформы RuКод 

## 📌 Содержание
- [Бизнес-задача](#-бизнес-задача)
- [Данные](#-данные)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Эксперименты и результаты](#-эксперименты-и-результаты)
- [Воспроизведение](#-воспроизведение)
- [Деплой](#-деплой)
- [Контакты](#-контакты)
- [Лицензия](#-лицензия)

## 🎯 Бизнес-задача
Предсказать, откликнется ли клиент на предложение банка вложить средства в инвестиционный продукт  
Реализовать и обучить модель классификации, которая на основе имеющихся данных сможет сделать предсказание о том, согласится ли клиент вложить средства в инвестиционный продукт.

## 📊 Данные
- **Источник**: Соревнование [RuКод — «Найди инвестора»](https://rucode.net/ai_champ/)
- **Особенность реализации**:  
  Проект использует **синтетические данные**, сгенерированные на основе описания признаков из условий соревнования.  
  Реальные данные не применялись — использованы только названия и типы признаков.  
  Это позволило полностью отработать **ML-инженерный пайплайн**: генерация данных → обучение модели (CatBoost) → деплой в Docker → веб-интерфейс для инференса.
- **Файлы**:
  - `data.py` — сгенерированы синтетические данные для обучения модели.
  - `train.py` — обучение модели CatBoost на синтетических данных.
  - `Model_training.py` — запуск обучения модели.
- **Признаки** (примерный состав):

| Признак | Тип | Описание |
|--------|------|----------|
| `customer_id` | int | Уникальный идентификатор клиента |
| `age` | int | Возраст клиента (в годах) |
| `balance` | float | Текущий баланс на счетах клиента (в рублях) |
| `risk_profile` | str | профиль риска клиента, оценка его отношения к риску, категориальный признак, принимает значения `low`, `medium`, `high` |
| `marketing_channel` | str | канал, по которому связываются с клиентом, категориальный признак, принимает значения `email`, `sms`, `push`, `call` |
| `offer_amount` | float | сумма, которую предлагается вложить клиенту |
| `previous_investments` | int | делал ли клиент вложения ранее |
| `responded_before` | bool | отвечал ли клиент ранее (`True`/`False`) |
| `membership_tier` | str | уровень привилегий клиента, категориальный признак, принимает значения: `basic`, `silver`, `gold`, `platinum` |
| `accepted` | bool | **Целевая переменная**: принял ли клиент текущее предложение (`True` = 1, `False` = 0) |

- **Особенности обработки**:
 - **Пропуски**: Данные соревнования были предоставлены без пропущенных значений
 - **Категориальные признаки**: явно указаны при инициализации CatBoost (`cat_features`), включая: `risk_profile`, `marketing_channel`, `membership_tier`
  - **Числовые признаки**: оставлены «как есть» — CatBoost автоматически обрабатывает их без предварительной нормализации или масштабирования.

## 📂 Структура проекта
```
rucode-find-investor/
├── app/ # FastAPI приложение
├── data/ # Предобработанные данные (.pkl)
├── img/ # изображение RuКод
├── models/ # Сохранённые модели (joblib/pickle)
├── notebooks/ # ноутбук для экспериментов
├── src/ # Основной production-код
│ ├── init.py
│ ├── data.py # Генерирование синтетических данных
│ ├── train.py # Обучение модели
│ └── model_training.py # Скрипт запуска обучения модели
├── tests/ # Юнит-тесты
├── config.yaml # Гиперпараметры и пути
├── requirements.txt # Фиксированные зависимости
├── Dockerfile # Контейнеризация
├── README.md # Описание проекта
└── pyproject.toml        # Конфигурация проекта и зависимостей (PEP 621)
```

## ⚡ Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone https://github.com/slesaradmi93/rucode-find-investor.git
cd rucode-find-investor

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/MacOS
# venv\Scripts\activate   # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить обучение модели
python -m src.model_training

# 5. Собрать Docker образ
docker build -t find-investor:1.0 .

# 6. Запустить Docker образ
docker run --name my-investor-app find-investor:1.0

# 7. Перейти на сайт http://localhost:8000/

# 8. Заполнить формы пользователя выдуманными данными и проверить предсказание модели

```

## 📈 Эксперименты и результаты
**Результат**
F1 Score на валидации: 0.868861305990286
**Бaллы в контесте**
96.56

## 🔁 Воспроизведение

> ⚠️ **Важно**: проект реализован **исключительно на синтетических данных**, сгенерированных в соответствии с описанием признаков из соревнования RuКод «Найди инвестора».  
> **Цель проекта — не достичь высокой точности модели, а продемонстрировать полный ML-пайплайн**: от генерации данных и обучения до упаковки модели в Docker и развёртывания веб-API.

При запуске:
```bash
python -m src.model_training
```
вы получите:
- Генерацию обучающей и тестовой выборок (с фиксированным seed)
- Обучение модели CatBoost
- Сохранение модели в `models/model.pkl`
- Оценку F1-score на валидации

Результаты (включая возможное переобучение) **не отражают качество на реальных данных**, так как последние недоступны. Акцент сделан на **инженерной части**: структура кода, воспроизводимость, деплой и документация.

Для запуска всего пайплайна — следуйте инструкции в разделе [Быстрый старт](#-быстрый-старт).

- **Python**: 3.12.7  
- **Зависимости**: см. `requirements.txt` (включая `catboost==1.2.8`, `scikit-learn==1.8.0`)  
- **Random seed**: 42 (используется при генерации синтетических данных и разбиении выборки)  
- **Метрика**: F1-score, вычисляется с помощью `sklearn.metrics.f1_score(y_test, test_preds)`

Поскольку данные генерируются синтетически, но с фиксированным seed, любой запуск команды из [Быстрого старта](#-быстрый-старт) приведёт к **одинаковой модели и F1 ≈ 0.985493**.

> 💡 Для точного соответствия убедитесь, что используете ту же версию Python и библиотек.

## 🚀 Деплой

Модель упакована в **Docker-контейнер** с веб-интерфейсом на **FastAPI**.  
Frontend реализован через статический HTML-шаблон из папки `templates/`, подключённый через `FastAPI + Jinja2`.  

После запуска:
- Сервер доступен по адресу: `http://localhost:8000`
- Отображается форма для ввода параметров клиента (возраст, баланс, риск-профиль и др.)
- Результат предсказания отображается прямо на странице: **«Примет предложение»** или **«Не примет»**

Это демонстрирует полный цикл: от ML-модели до пользовательского интерфейса в production-подобной среде.

## 📧 Контакты
- Автор: Слесарев Александр Дмитриевич
- Email: QA.Slesar@yandex.ru
- LinkedIn: 
- Дата завершения: Январь 2026

## 📜 Лицензия
Проект распространяется под лицензией [MIT](LICENSE).